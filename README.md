# The place to share ideas

1. GraphQL
  - build GraphQL server (Language?)
  - consume GraphQL data in mobile app (Flutter?)
2. Flutter
   - Build a real life app with Flutter (Think about what it can be...)
